/*STL(Standard template library)
library of what?
> Generic classes and functions

why use STL?
>Reuse: well tested components
>time saving

component of STL?
>containers->stores data, use template classes
>algorithm->sorting, searching, use template functions, procedure to process data
>iterators->object points to an element in a container, handled just like pointers, connects algorithm with containers

STL is used because its a good idea not to reinvert the wheel.

*/